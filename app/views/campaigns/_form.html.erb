<%= simple_form_for(@campaign, html: { class: "form-horizontal", :multipart => true }) do |f| %>
  <%= f.error_notification %>

  <%= f.input :name, input_html: { rows: "3" } %>
  <br />
  <%= f.button :submit, class: "btn btn-primary" %>
 
<% end %>
